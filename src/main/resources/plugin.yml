{
  "name": "LuckyCrates",
  "authors": [
    "emilyy-dev"
  ],
  "version": "${pluginVersion}",
  "main": "io.github.emilyydev.luckycrates.LuckyCratesPlugin",
  "load": "POSTWORLD",
  "depend": [ "Vault" ],

  "permissions": {
    "luckycrates.use": {
      "default": "op",
      "description": "Permission used to allow a player to execute the /luckycrates command",
      "children": {}
    }
  }
}
