{
  "name": "LuckyCrates",
  "authors": [
    "Fefo"
  ],
  "version": "${pluginVersion}",
  "main": "com.github.fefo.luckycrates.LuckyCratesPlugin",
  "load": "POSTWORLD",

  "permissions": {
    "luckycrates.use": {
      "default": "op",
      "description": "Permission used to allow a player to execute the /luckycrates command",
      "children": {}
    }
  }
}
